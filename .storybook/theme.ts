import { create } from '@storybook/theming/create';

const theme = create({
  base: 'light',
  brandTitle: 'Tanzmoment UI',
  colorPrimary: '#2F4937',
  colorSecondary: '#B5805D'
});

export default theme;
